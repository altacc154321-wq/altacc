services:
  - type: web
    name: regicide-arena
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install && npm test
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "22"
